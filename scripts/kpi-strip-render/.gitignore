# Generated by check:kpi-strip — never commit these.
# app.css especially: a committed stylesheet goes stale the moment a class
# changes, and a stale one makes the harness fail correct code and pass broken
# code. See the header of scripts/check-kpi-strip.mjs.
app.css
bundle.js
shot_*.png
