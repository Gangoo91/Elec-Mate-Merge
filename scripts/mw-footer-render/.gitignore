# Generated by check:mw-footer — never commit these.
# app.css in particular: a committed stylesheet goes stale the moment a class
# changes, and a stale one makes the harness fail correct code and pass broken
# code. See the header of scripts/check-mw-footer.mjs.
app.css
bundle.js
shot_*.png
