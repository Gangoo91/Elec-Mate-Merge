<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ (API 31+) data extraction rules. Excludes all app-private data
  from both cloud backup and device-to-device transfer to prevent leaking
  Supabase auth tokens, cached certs, and localStorage to Google Drive or a
  new device. Sensitive data should always be re-fetched from Supabase after
  a restore / migration so the access path goes through the auth flow.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
