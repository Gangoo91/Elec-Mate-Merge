
import { SectionData } from '../courseTypes';

// Placeholder for installation methods content - ready for new content
export const installationMethodsSections: SectionData[] = [];

// Export for backward compatibility
export const installationMethodsContent = installationMethodsSections;
