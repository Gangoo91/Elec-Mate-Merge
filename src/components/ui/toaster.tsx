/**
 * Legacy shadcn Toaster — no longer used.
 * All toasts now render via Sonner (<SonnerToaster /> in App.tsx).
 * This file is kept as a no-op so any stale imports don't crash the app.
 */
export function Toaster() {
  return null;
}
