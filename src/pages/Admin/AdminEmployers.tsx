/**
 * Employers. The employer scheme (30% apprentice / 40% electrician company
 * codes) on the same page as Colleges: conversations from the tracker,
 * take-up from Stripe, owner/manager accounts from Bulk create.
 */
import { SchemePage } from '@/pages/Admin/AdminColleges';

export default function AdminEmployers() {
  return <SchemePage scheme="employer" />;
}
